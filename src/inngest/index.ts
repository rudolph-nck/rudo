// Inngest barrel export — re-exports the client for easy import.
// Functions are registered via the API route, not imported individually.

export { inngest } from "./client";
export type { Events } from "./events";
